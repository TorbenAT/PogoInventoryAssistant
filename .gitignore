bin/
obj/
out/
.vs/
.idea/
*.user
*.suo
*.zip

# Real Android device and inventory data must stay local. Explicitly committed iPhone pretest fixtures are handled separately.
captures/
local-data/
private/
tools/platform-tools/
# tessdata_best is ~15MB (over the "commit if small" line); tools/tessdata/
# (tessdata_fast, ~4MB) is committed instead. See docs/HEADER_OCR.md for the
# manual download instructions if tessdata_best is needed.
tools/tessdata-best/
*.db
*.db-*
*.sqlite
*.sqlite3
*.log

# Real screen calibration stays local while the repository is public.
data/screen-profile.local.json
data/automation-profile.local.json
data/screen-fixtures-real/
# Private calibration workspace metadata and generated local files.
**/.pogo-private-calibration
**/fixture-manifest.local.json
**/anchor-plan.local.json
**/screen-profile.local.json
**/capture-plan.local.json
**/capture-session.local.json
**/incoming/**/*.png
