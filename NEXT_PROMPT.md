# Continuation prompt

Use this text with the latest repository after version 0.10.1 is green.

---

I am building Pogo Inventory Assistant in C# and .NET 8.

Open and inspect the repository before changing anything. Treat `PROJECT_STATE.md` as the source of truth. Read `docs/GUARDRAILS.md`, `docs/IPHONE_IMAGE_PRETEST.md`, `docs/CALCY_PROVIDER_VERIFICATION.md` and `docs/AUTOMATIC_NAVIGATION.md`.

Current version: 0.10.1.

Accepted before this task:

- automatic allow-listed Android navigation
- automatic local core-profile bootstrap
- structured nullable Calcy observations
- Calcy package and live-check probe
- twenty-case zero-false-Complete provider gate
- 24 committed iPhone screenshots
- real pretest result of 23 decoded screenshots, one geometry group and four visual clusters
- isolated decode failures retained as diagnostics instead of invalidating a strong fixture set
- 86 self-tests

First verify that GitHub Actions is green. Record the exact rejected iPhone filename and decoder error printed by the command.

Next milestone: build a visual-region discovery report from the committed iPhone screenshots.

Required work:

1. Identify stable and changing normalised regions across consecutive decoded images.
2. Produce per-region variance and transition data in JSON and CSV.
3. Suggest candidate regions for screen state, Pokémon identity, CP/name and appraisal content.
4. Keep suggestions explicitly provisional until supported by evidence.
5. Exclude the rejected PNG from visual calculations but retain its diagnostics.
6. Add deterministic replay tests using decoded images.
7. Do not claim iPhone results validate Android coordinates or Calcy behaviour.
8. Add no new phone input action.
9. Update project state, continuation prompt, release notes and validation report.

When the fixed Android phone becomes available, run the real Calcy probe before implementing a production provider.
